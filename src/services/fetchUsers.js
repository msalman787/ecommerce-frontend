USER_URL = ""

export function logIn({ username, password }) {
    return fetch(USER_URL, {

    }).json()
}

export function addFavorite({ product_id }) {
    return fetch(USER_URL, {

    }).json()
}